
{
    let fullname: string = "Miroslav Stoimenov";
    let age: number = 38;

    console.log(`Hello! I am ${fullname}, ${age} years old`);
}


{
    let num1: number = 8;
    let num2: number = 4;

    let addition: number = num1 + num2;
    let subtraction: number = num1 - num2;

    console.log(`Addition: ${addition}`);
    console.log(`Subtraction: ${subtraction}`);
}


{
    let num: number = 12;
    let isWithinRange: boolean = num >= 10 && num <= 15;

    console.log(`Is the number within range? ${isWithinRange}`);
}
